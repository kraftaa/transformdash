# Docker Compose Environment Variables
# This file is specifically for Docker deployments
# Copy to .env.docker and set JWT_SECRET_KEY

# JWT Secret Key for authentication (REQUIRED)
# Generate: python -c 'import secrets; print(secrets.token_urlsafe(32))'
JWT_SECRET_KEY=

# PostgreSQL Configuration (Docker defaults - don't change unless needed)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD_IN_PRODUCTION
POSTGRES_DB=transformdash

# Optional: pgAdmin Configuration
PGADMIN_EMAIL=admin@transformdash.local
PGADMIN_PASSWORD=CHANGE_THIS_PASSWORD_IN_PRODUCTION
